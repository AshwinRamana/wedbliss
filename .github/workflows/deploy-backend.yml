name: Deploy Backend

on:
  push:
    branches: [main]
    paths:
      - 'backend/**'
      - '.github/workflows/deploy-backend.yml'
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Deploy to EC2 via SSH
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.EC2_HOST }}
          username: ${{ secrets.EC2_USER }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            set -e
            cd ~/wedbliss
            git pull origin main
            cd backend
            npm install --production
            # Env lives on the server (~/wedbliss/.env.local). Do not overwrite
            # from CI — empty/missing GitHub secrets would wipe Gmail/Cloudflare/Razorpay.
            pm2 restart wedbliss-api --update-env || pm2 start server.js --name wedbliss-api
            curl -sf http://127.0.0.1:4000/health

      - name: Send Email Notification
        if: always()
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: smtp.gmail.com
          server_port: 465
          username: ${{ secrets.MAIL_USERNAME }}
          password: ${{ secrets.MAIL_PASSWORD }}
          subject: WedBliss Deploy Result - ${{ github.workflow }}
          to: ashwinramana7@gmail.com
          from: GitHub Actions
          body: The deployment job '${{ github.job }}' in workflow '${{ github.workflow }}' finished with status '${{ job.status }}'.
